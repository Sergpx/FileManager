#pragma once
#include <SFML/Graphics.hpp>

const int BUTTON_SIZE = 100;
const int FONT_SIZE = 24;

const int WINDOW_W = 800;
const int WINDOW_H = 600;

const int FRAME_W = 300;
const int FRAME_H = WINDOW_H;

const sf::Vector2f leftFrameCoord = sf::Vector2f(0, 0);
const sf::Vector2f rightFrameCoord = sf::Vector2f(FRAME_W + 200, 0);